const discounts = require('./discounts');
const products = require('./products');
const tasks = require('./tasks');
const colors = require('./colors');
const types = require('./types');

module.exports = { discounts, products, tasks, colors, types };
